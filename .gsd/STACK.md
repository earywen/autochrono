# Technology Stack

> Generated by /map on 2026-02-20
> **Target Scope:** `outlook-module/generator/` (OutlookToolGen)

## Runtime
| Technology | Version | Purpose |
|------------|---------|---------|
| Python | 3.x | Core application logic, VBA templating, file/clipboard operations |
| WebView2 / EdgeChromium | Latest Windows | Rendering engine for the PyWebView graphical interface |
| VBA (Visual Basic for Applications) | Outlook/Office | Mail interception, Excel manipulation, Windows Shell interactions, and file archiving in the Outlook environment |

## Production Dependencies
| Package | Version | Purpose |
|---------|---------|---------|
| pywebview | >=5.0.0 | Creates the native window and facilitates JS <-> Python bridging. Uses HTTP Server mode internally for stability. |
| pyinstaller | >=6.0.0 | Bundling the Python app into a standalone Windows `.exe` (`Generator.spec`) |
| pyperclip | N/A | (Optional/Dynamic Import) Used for clipboard access. Fallback uses `clip.exe` subprocess. |

## Development Dependencies
| Package | Version | Purpose |
|---------|---------|---------|
| openpyxl | N/A | (Not directly used in the generator module, but present in root for Excel debugging) |

## Infrastructure / External Systems
| Service | Provider | Purpose |
|---------|----------|---------|
| Microsoft Office (Outlook Object Model)| Microsoft | Execution context for the generated VB scripts (Macros & Events) |
| Microsoft Office (Excel COM) | Microsoft | Interop reading/writing for the Chrono numbering system |
| Windows API (user32.dll/kernel32.dll)| Microsoft | Native clipboard manipulation in VBA for pasting "REF" tags |
| Windows Shell API (Shell.Application) | Microsoft | Opening folder selection dialogs directly within VBA (used in AR archiving) |
| TailwindCSS | CDN | Used within `ui/index.html` for layout, styling, and glassmorphism UI |

## Configuration / Build
| Variable | Purpose | Required |
|----------|---------|----------|
| `Generator.spec` | PyInstaller configuration file | Yes (for building) |
| `Trigramme` | User's 3-letter identifier for Chrono network folder naming | Yes (User input - Chrono) |
| `Dossier Racine Chrono`| Base network path for archiving Chrono files | Yes (User input - Chrono) |
| `Fichier Excel Chrono` | Network/Local path to the Chrono tracking sheet | Yes (User input - Chrono) |
| `Nom du Chef de Projet`| Default name injected into AR tables | Yes (User input - AR) |
| `Telephone` | Default phone injected into AR tables | Yes (User input - AR) |
